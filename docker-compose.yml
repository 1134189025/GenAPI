services:
  app:
    image: ghcr.io/1134189025/genapi:latest
    container_name: genapi
    restart: unless-stopped
    ports:
      - "3000:80"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/app/deployment:ro
    environment:
      # 存储后端配置 (可选值: json, sqlite, postgres, git)
      - STORAGE_BACKEND=json

      # Docker 部署网页更新配置：通过 Docker socket 更新 app 服务镜像。
      - GENAPI_DEPLOYMENT_MODE=docker
      - GENAPI_BUILD_TYPE=docker
      - GENAPI_UPDATE_COMPOSE_DIR=/app/deployment
      - GENAPI_UPDATE_COMPOSE_FILE=docker-compose.yml
      - GENAPI_UPDATE_HOST_COMPOSE_DIR=${PWD}
      - GENAPI_UPDATE_HOST_DATA_DIR=${PWD}/data
      - GENAPI_UPDATE_SERVICE=app
      - GENAPI_UPDATE_HELPER_IMAGE=docker:28-cli
      # 可选：用于访问 GitHub Releases API，避免匿名请求限流。
      # - GITHUB_TOKEN=ghp_xxxxxxxxxxxx

      # 数据库配置 (当 STORAGE_BACKEND=sqlite/postgres 时使用)
      # - DATABASE_URL=postgresql://user:password@host:5432/dbname
      # - DATABASE_URL=sqlite:///app/data/accounts.db

      # Git 仓库配置 (当 STORAGE_BACKEND=git 时使用)
      # - GIT_REPO_URL=https://github.com/user/repo.git
      # - GIT_TOKEN=ghp_xxxxxxxxxxxx
      # - GIT_BRANCH=main
      # - GIT_FILE_PATH=accounts.json

      # 基础 URL (可选)
      # - GENAPI_BASE_URL=https://your-domain.com
